# Normalize line endings to LF across the repo (prevents CRLF/LF churn on Windows).
* text=auto eol=lf

# Binary assets: never normalize.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.pdf binary
